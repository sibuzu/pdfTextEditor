feat: 實作單頁工具列與放大鏡功能 (Page Toolbar & Magnifier)

主要變更：
1. **單頁工具列 (Per-Page Toolbar)**：
   - 新增「全部回復」、「顯示/隱藏文字框」、「放大鏡」按鈕於每頁標題旁。
   - 修正工具列排版，確保不換行並增加間距。
   - 將全域顯示切換 (Global Toggle) 改為單頁獨立控制。

2. **放大鏡功能 (Magnifier)**：
   - 實作高解析度局部放大鏡，滑鼠游標移動時顯示原始圖片細節。
   - 支援點擊切換啟用/停用狀態。

3. **編輯器互動優化 (Interaction Improvements)**：
   - 點擊頁面背景可自動取消選取文字方塊 (Deselect)。
   - 修正選取區塊時右側面板資料未更新的 Bug (移除無用的 selectedPage 元素)。
   - 當無選取時，右側面板顯示頁面資訊但鎖定編輯欄位。

4. **樣式調整 (CSS)**：
   - 新增 `.page-toolbar`, `.magnifier-lens` 等樣式。
   - 優化 `.page-header` 佈局與右側輸入框間距。
