feat: 實作文字渲染與斜體支援 (Text Rendering & Italic Support)

詳細變更內容：

1. **文字渲染引擎 (Text Rendering Engine)**：
   - **核心邏輯 (`execution/editor_engine.py`)**：
     - 在 Inpainting 去背後，新增 `PIL.ImageDraw` 文字繪製流程。
     - 實作 `get_optimal_font_scale`：自動計算最佳字體大小以適應 Bounding Box 寬高。
     - 預設字體採用 `NotoSansTC-Regular.ttf` 以支援繁體中文顯示。
     - 新增詳細 Debug Log：記錄使用的字體、大小、顏色與座標。

2. **斜體文字支援 (Italic Support)**：
   - **後端擴充**：
     - `ApplyEditRequest` (`server.py`) 新增 `is_italic` (bool) 欄位。
     - 引擎層支援字體切換：若 `is_italic=True`，則自動切換至 `Roboto-Italic.ttf` (主要針對英文情境)。
   - **前端介面**：
     - HTML (`templates/index.html`) 新增「Italic (斜體)」核取方塊。
     - JS (`static/js/editor.js`) 在發送編輯請求時讀取並傳送核取方塊狀態。

3. **其他修正**：
   - 伺服器端整合了完整的「去背 -> 繪圖」流程。
